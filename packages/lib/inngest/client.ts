import { Inngest } from 'inngest';

/**
 * Inngest client for orchestrating AI research workflows
 * Event types are defined in ./types.ts
 */
export const inngest = new Inngest({
  id: 'ai-research-generator',
});
